{
  "pluginId": "qmeshlab.filter.texture_defragmentation",
  "references": [
    {
      "id": "maggiordomo2021texture",
      "type": "article-journal",
      "title": "Texture Defragmentation for Photo-Reconstructed 3D Models",
      "author": [
        {
          "family": "Maggiordomo",
          "given": "Andrea"
        },
        {
          "family": "Cignoni",
          "given": "Paolo"
        },
        {
          "family": "Tarini",
          "given": "Marco"
        }
      ],
      "container-title": "Computer Graphics Forum",
      "volume": "40",
      "issue": "2",
      "page": "65-78",
      "publisher": "Wiley",
      "issued": {
        "date-parts": [
          [
            2021
          ]
        ]
      },
      "DOI": "10.1111/cgf.142615",
      "URL": "https://diglib.eg.org/items/b3e092cd-5a96-4575-975d-af22795e870b"
    }
  ],
  "filters": [
    {
      "id": "apply_texmap_defragmentation",
      "categories": ["Parametrization/Defragmentation", "Texture"],
      "name": "Defragment Texture Atlas",
      "pythonName": "defragment_texture_map",
      "referenceIds": [
        "maggiordomo2021texture"
      ],
      "shortDescription": "Reduce texture atlas fragmentation by merging compatible charts and resampling the texture map.",
      "longDescriptionMarkdown": "Reduces texture fragmentation by merging compatible atlas charts, repacking the resulting charts, and resampling the associated texture images onto the optimized atlas.\n\nThis QMeshLab port vendors the upstream TextureDefrag reference implementation from `https://github.com/maggio-a/texture-defrag` and adapts it to QMeshLab's mesh-owned texture model. The filter creates a new mesh layer named `texdefrag_<source>` rather than overwriting the input mesh, preserving the original atlas for comparison.\n\nImplementation note: the original reference code used OpenGL to rasterize the new texture atlas. QMeshLab isolates that step behind the original `RenderTexture` boundary; this first integration uses an internal image renderer so the algorithm can run from the filter framework without requiring an OpenGL context. The renderer boundary is intentionally narrow so a QRhi backend can replace it cleanly.",
      "tags": [
        "texture",
        "atlas",
        "defragmentation",
        "uv",
        "bibliography"
      ],
      "inputDomain": "SingleMesh",
      "outputDomain": "NewMeshes",
      "inputRequirements": {
        "requireVertices": true,
        "requireFaces": true,
        "requirePerWedgeTexCoords": true,
        "requireTextures": true
      },
      "inputPrepare": [
        "FF",
        "FNorm",
        "VNorm"
      ],
      "parameters": [
        {
          "id": "matchingThreshold",
          "label": "Matching Error Threshold",
          "help": "Threshold on the seam alignment error. Higher values allow more seams to be considered compatible, which can reduce fragmentation, but may increase runtime and risk more distortion. MeshLab default: `2.0`.",
          "group": "main",
          "type": "double",
          "default": 2.0,
          "min": 0.0,
          "max": 1000000.0,
          "decimals": 3
        },
        {
          "id": "boundaryTolerance",
          "label": "Seam to chart-boundary-length tolerance",
          "help": "Cutoff on the minimum fractional seam length relative to the chart perimeter. Seams with lower fractional length are not merged, helping keep chart borders compact. MeshLab default: `0.2`.",
          "group": "main",
          "type": "double",
          "default": 0.2,
          "min": 0.0,
          "max": 1.0,
          "decimals": 3
        },
        {
          "id": "distortionTolerance",
          "label": "Local ARAP distortion tolerance",
          "help": "Local UV optimization distortion tolerance when merging a seam. If local ARAP energy exceeds this value, the merge is reverted. Smaller values preserve the original parameterization more strictly; larger values can merge more charts. MeshLab default: `0.5`.",
          "group": "main",
          "type": "double",
          "default": 0.5,
          "min": 0.0,
          "max": 1000000.0,
          "decimals": 3
        },
        {
          "id": "globalDistortionTolerance",
          "label": "Global ARAP distortion tolerance",
          "help": "Global atlas ARAP distortion tolerance when accepting a seam merge. If the global atlas energy exceeds this value, the merge is reverted. This is usually kept much smaller than the local tolerance. MeshLab default: `0.025`.",
          "group": "main",
          "type": "double",
          "default": 0.025,
          "min": 0.0,
          "max": 1000000.0,
          "decimals": 4
        },
        {
          "id": "uvReductionLimit",
          "label": "UV Length Target (%)",
          "help": "Target UV boundary length reduction, expressed as a percentage of the input UV boundary length. The algorithm stops once this reduction has been reached, or when no further seams can be merged. `0` means no target reduction stop condition. MeshLab default: `0`.",
          "group": "main",
          "type": "double",
          "default": 0.0,
          "min": 0.0,
          "max": 100.0,
          "decimals": 3
        },
        {
          "id": "offsetFactor",
          "label": "Local expansion coefficient",
          "help": "Coefficient controlling the extension of the local UV optimization area. Larger values can make defragmentation more effective by giving the optimizer more room, but increase geometric optimization cost and runtime. MeshLab default: `5.0`.",
          "group": "advanced",
          "type": "double",
          "default": 5.0,
          "min": 0.0,
          "max": 1000000.0,
          "decimals": 3
        },
        {
          "id": "timelimit",
          "label": "Time limit (seconds)",
          "help": "Time limit for the defragmentation process. `0` means unlimited. This is useful for very large atlases where chart merging can be expensive. MeshLab default: `0`.",
          "group": "advanced",
          "type": "double",
          "default": 0.0,
          "min": 0.0,
          "max": 1000000.0,
          "decimals": 3
        },
        {
          "id": "randomSeed",
          "label": "Random seed",
          "help": "Zero draws a fresh seed on every run, so repeated applications differ; any other value makes the atlas packing exactly reproducible.",
          "group": "advanced",
          "type": "int",
          "default": 0,
          "min": 0,
          "max": 2147483647
        },
        {
          "id": "resampleTextures",
          "type": "bool",
          "label": "Resample textures",
          "default": true,
          "description": "Render the new atlas by resampling the original texture images. Turn it off to treat this as a parametrization-only filter: the layer comes back with the reorganized UV layout and no texture images, which is what you want when the texture is going to be baked again afterwards. The source images are still required either way -- the algorithm measures islands and distortion in texel space, so it needs their resolution before it can start.",
          "group": "main"
        }
      ]
    },
    {
      "id": "apply_small_islands_remover",
      "categories": ["Parametrization/Defragmentation", "Texture"],
      "name": "Merge Small Texture Islands",
      "pythonName": "small_islands_remover",
      "referenceIds": [
        "maggiordomo2021texture"
      ],
      "shortDescription": "Merge texture islands below a size threshold into neighbors sharing a seam, then repack and resample the atlas.",
      "longDescriptionMarkdown": "Attempts to reduce all texture islands within a given size by merging them with neighbors sharing a common seam. The procedure tries to avoid distortion and overlap introduced by the removal of islands. It shares the defragmentation pipeline, so it needs the texture images as well as the parametrization: UVs are scaled into pixel space before anything else runs, the surviving charts are repacked, and the new atlas is resampled from the originals. The layer therefore comes back with new texture images, not just moved UVs.",
      "tags": [
        "texture",
        "atlas",
        "defragmentation",
        "uv",
        "bibliography"
      ],
      "inputDomain": "SingleMesh",
      "outputDomain": "NewMeshes",
      "inputRequirements": {
        "requireVertices": true,
        "requireFaces": true,
        "requirePerWedgeTexCoords": true,
        "requireTextures": true
      },
      "inputPrepare": [
        "FF",
        "FNorm",
        "VNorm"
      ],
      "parameters": [
        {
          "id": "maxMultiplier",
          "label": "Max size multiplier\n(relative to median island border)",
          "help": "Islands whose UV boundary length is smaller than this factor times the median island boundary length are candidates for removal. A value of 1.0 targets all below-median islands; 0.2 targets only the absolute tiniest fragments; values above 1.0 also capture larger-than-median islands. Set to 0 to attempt removal of all islands.",
          "group": "main",
          "type": "double",
          "default": 1.0,
          "min": 0.0,
          "max": 1000000.0,
          "decimals": 3
        },
        {
          "id": "distortionMode",
          "label": "Distortion Mode",
          "help": "Specifies how aggressively the algorithm removes islands:\n**STRICT**: operations that introduce significant geometric distortion are immediately rejected. Prioritizes visual fidelity over compactness.\n**LOOSE**: distortion introduced by the removal of islands is ignored. Still, operations that introduce overlaps are rejected. Prioritizes layout compactness over quality.\n**UNSAFE**: ignores any distortion and intersection introduced by a merge operation. This mode leads to the most compact and fast result.\nNote that topologically incompatible merges are still skipped, as they cannot be parameterized.",
          "group": "main",
          "type": "enum",
          "default": "STRICT",
          "enumOptions": [
            {
              "id": "STRICT",
              "label": "STRICT"
            },
            {
              "id": "LOOSE",
              "label": "LOOSE"
            },
            {
              "id": "UNSAFE",
              "label": "UNSAFE"
            }
          ]
        },
        {
          "id": "targetTexCount",
          "label": "Target textures number",
          "help": "Specifies the maximum number of output textures that can be generated by the filter.\nIf set to zero the parameter is ignored and the algorithm employs the default packing strategy.",
          "group": "main",
          "type": "int",
          "default": 0,
          "min": 0,
          "max": 1000000
        },
        {
          "id": "timelimit",
          "label": "Time limit (seconds)",
          "help": "Time limit for the chart merging stage. `0` means unlimited. Note that it does not bound the atlas packing that follows, which on a heavily fragmented layout can be the slower half.",
          "group": "advanced",
          "type": "double",
          "default": 0.0,
          "min": 0.0,
          "max": 1000000.0,
          "decimals": 3
        },
        {
          "id": "quickRun",
          "label": "Quick execution",
          "help": "Speeds up the running time of the filter by never attempting again any rejected merge operation. <br> Although fast, it could lead to worst results.",
          "group": "main",
          "type": "bool",
          "default": false
        },
        {
          "id": "randomSeed",
          "label": "Random seed",
          "help": "Zero draws a fresh seed on every run, so repeated applications differ; any other value makes the atlas packing exactly reproducible.",
          "group": "advanced",
          "type": "int",
          "default": 0,
          "min": 0,
          "max": 2147483647
        },
        {
          "id": "resampleTextures",
          "type": "bool",
          "label": "Resample textures",
          "default": true,
          "description": "Render the new atlas by resampling the original texture images. Turn it off to treat this as a parametrization-only filter: the layer comes back with the reorganized UV layout and no texture images, which is what you want when the texture is going to be baked again afterwards. The source images are still required either way -- the algorithm measures islands and distortion in texel space, so it needs their resolution before it can start.",
          "group": "main"
        }
      ]
    },
    {
      "id": "pack_uv_charts",
      "pythonName": "pack_uv_charts",
      "name": "Pack UV Charts",
      "categories": [
        "Parametrization/Atlas Packing",
        "Texture"
      ],
      "shortDescription": "Lay the existing UV charts out again in a single atlas, using a choice of vcglib packing algorithms.",
      "longDescriptionMarkdown": "Repacks the atlas without changing the charts themselves: no merging, no reparametrization, just a new layout for the islands the mesh already has.\n\nFour packers are offered, and they trade quality against time very differently:\n\n- **Rasterized, scaled to fit** rasterizes each chart outline and searches for the largest scale at which everything fits. The tightest packing, and the slowest.\n- **Rasterized, best effort** keeps the charts at their current scale and places as many as it can in the atlas you asked for.\n- **Axis-aligned rectangles** packs each chart's bounding box. Fast and loose.\n- **Object-oriented rectangles** packs each chart's minimum-area oriented box, rotating the chart to match. Usually tighter than axis-aligned for nothing like the cost of rasterizing.\n\nThe *Try chart permutations* option is worth knowing about. The rasterized packers can retry the layout with reshuffled chart orders, which costs five packing passes per similarly sized chart -- on an atlas whose islands are all much of a muchness that is five passes per chart, and it dominates the run. It is off here by default.",
      "inputRequirements": {
        "requireVertices": true,
        "requireFaces": true,
        "requirePerWedgeTexCoords": true
      },
      "inputPrepare": [
        "FF",
        "FNorm",
        "VNorm"
      ],
      "parameters": [
        {
          "id": "algorithm",
          "label": "Packing algorithm",
          "type": "enum",
          "group": "main",
          "default": "rasterized_scaled",
          "enumOptions": [
            {
              "id": "rasterized_scaled",
              "label": "Rasterized, scaled to fit"
            },
            {
              "id": "rasterized_best_effort",
              "label": "Rasterized, best effort"
            },
            {
              "id": "axis_aligned_rect",
              "label": "Axis-aligned rectangles"
            },
            {
              "id": "object_oriented_rect",
              "label": "Object-oriented rectangles"
            }
          ],
          "help": "Which vcglib packer lays the charts out."
        },
        {
          "id": "textureSize",
          "label": "Atlas size (pixels)",
          "type": "int",
          "group": "main",
          "default": 1024,
          "min": 16,
          "max": 16384,
          "help": "Side of the square atlas the charts are packed into."
        },
        {
          "id": "gutterWidth",
          "label": "Gutter (pixels)",
          "type": "int",
          "group": "main",
          "default": 4,
          "min": 0,
          "max": 256,
          "help": "Padding kept around each chart, so bilinear filtering cannot bleed between neighbours."
        },
        {
          "id": "rotationNum",
          "label": "Rotations tried",
          "type": "int",
          "group": "advanced",
          "default": 4,
          "min": 1,
          "max": 64,
          "help": "How many orientations each chart is tried at. Rasterized packers only."
        },
        {
          "id": "permutations",
          "label": "Try chart permutations",
          "type": "bool",
          "group": "advanced",
          "default": false,
          "help": "Retry the layout with reshuffled chart orders. Costs five packing passes per similarly sized chart, so on a uniform atlas it is five per chart and it dominates the run. Rasterized packers only."
        },
        {
          "id": "resampleTextures",
          "type": "bool",
          "label": "Resample textures",
          "default": true,
          "description": "Render the new atlas by resampling the original texture images. Turn it off to treat this as a parametrization-only filter: the layer comes back with the reorganized UV layout and no texture images, which is what you want when the texture is going to be baked again afterwards. The source images are still required either way -- the algorithm measures islands and distortion in texel space, so it needs their resolution before it can start.",
          "group": "main"
        },
        {
          "id": "randomSeed",
          "label": "Random seed",
          "help": "Zero draws a fresh seed on every run, so repeated applications differ; any other value makes the atlas packing exactly reproducible.",
          "group": "advanced",
          "type": "int",
          "default": 0,
          "min": 0,
          "max": 2147483647
        }
      ]
    }
  ]
}
