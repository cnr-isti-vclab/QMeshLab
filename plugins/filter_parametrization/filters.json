{
  "pluginId": "qmeshlab.filter.parametrization",
  "filters": [
    {
      "id": "compute_texcoord_parametrization_harmonic",
      "menuPath": "Texture",
      "name": "Harmonic Parametrization",
      "pythonName": "compute_texcoord_parametrization_harmonic",
      "shortDescription": "Compute a single-patch fixed-boundary harmonic UV parametrization using libigl.",
      "longDescriptionMarkdown": "Computes a single-patch fixed-boundary harmonic parametrization of the current mesh and stores the result as per-vertex texture coordinates. The mesh must have a boundary. If the mesh already has per-wedge texture coordinates, they are synchronized from the new per-vertex coordinates so the result is immediately visible in texture and UV views.",
      "tags": [
        "texture",
        "uv",
        "parametrization",
        "harmonic",
        "libigl"
      ],
      "inputDomain": "SingleMesh",
      "outputDomain": "ModifyCurrentMesh",
      "inputRequirements": {
        "requireVertices": true,
        "requireFaces": true
      },
      "inputPrepare": [
        "VTex"
      ],
      "parameters": [
        {
          "id": "harm_function",
          "label": "N-harmonic function",
          "help": "Order of the harmonic function. `1` is harmonic, `2` is biharmonic, `3` is triharmonic, and so on.",
          "group": "main",
          "type": "int",
          "default": 1,
          "min": 1,
          "max": 16
        }
      ],
      "outputModifies": [
        "VT",
        "WT"
      ]
    },
    {
      "id": "compute_texcoord_parametrization_least_squares_conformal_maps",
      "menuPath": "Texture",
      "name": "Least Squares Conformal Maps Parametrization",
      "pythonName": "compute_texcoord_parametrization_least_squares_conformal_maps",
      "shortDescription": "Compute a Least Squares Conformal Maps UV parametrization using libigl.",
      "longDescriptionMarkdown": "Computes a Least Squares Conformal Maps parametrization of the current mesh and stores the result as per-vertex texture coordinates. The mesh must have a boundary. If the mesh already has per-wedge texture coordinates, they are synchronized from the new per-vertex coordinates so the result is immediately visible in texture and UV views.",
      "tags": [
        "texture",
        "uv",
        "parametrization",
        "lscm",
        "libigl"
      ],
      "inputDomain": "SingleMesh",
      "outputDomain": "ModifyCurrentMesh",
      "inputRequirements": {
        "requireVertices": true,
        "requireFaces": true
      },
      "inputPrepare": [
        "VTex"
      ],
      "parameters": [],
      "outputModifies": [
        "VT",
        "WT"
      ]
    }
  ]
}
